interface:
  display_name: "Verification Before Completion"
  short_description: "Verify before claiming a task is done"
  default_prompt: "Use $verification-before-completion to verify the change before calling it done."
